{
  "name": "claude-code-proxy",
  "displayName": "Claude Code Proxy",
  "description": "管理 Claude Code 配置切换 + 本地 LLM 代理（重试 503/10310）+ workspace 隔离 CLI 会话（派生节点功能已移除）。",
  "version": "1.3.5",
  "publisher": "zaczh",
  "repository": {
    "type": "git",
    "url": "https://github.com/yananzhi/claude-code-proxy.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "bin": {
    "claude-code-proxy": "./standalone/cli.js"
  },
  "files": [
    "out/",
    "standalone/",
    "proxy/server.js",
    "proxy/config-store.js",
    "proxy/trace-store.js",
    "proxy/logger.js",
    "proxy/package.json",
    "proxy/web/",
    "package.json",
    "standalone/package.json",
    "README.md"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claude-code-proxy",
          "title": "Claude Code Proxy",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "claude-code-proxy": [
        {
          "id": "claude-code-proxy.configs",
          "name": "LLM Configs"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "claude-code-proxy.newConfig",
          "when": "view == claude-code-proxy.configs",
          "group": "navigation"
        },
        {
          "command": "claude-code-proxy.refresh",
          "when": "view == claude-code-proxy.configs",
          "group": "navigation"
        },
        {
          "command": "claude-code-proxy.toggleProxyBackupOn",
          "when": "view == claude-code-proxy.configs && !claude-code-proxy.proxyToggleEnabled",
          "group": "navigation"
        },
        {
          "command": "claude-code-proxy.toggleProxyBackupOff",
          "when": "view == claude-code-proxy.configs && claude-code-proxy.proxyToggleEnabled",
          "group": "navigation"
        },
        {
          "command": "claude-code-proxy.exportConfigs",
          "when": "view == claude-code-proxy.configs"
        },
        {
          "command": "claude-code-proxy.importConfigs",
          "when": "view == claude-code-proxy.configs"
        },
        {
          "command": "claude-code-proxy.launchWorkspaceClaude",
          "when": "view == claude-code-proxy.configs && workspaceFolderCount > 0",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "claude-code-proxy.switchConfig",
          "when": "view == claude-code-proxy.configs && viewItem == config",
          "group": "inline@1"
        },
        {
          "command": "claude-code-proxy.editConfig",
          "when": "view == claude-code-proxy.configs && viewItem == config",
          "group": "inline@2"
        },
        {
          "command": "claude-code-proxy.deleteConfig",
          "when": "view == claude-code-proxy.configs && viewItem == config",
          "group": "inline@3"
        },
        {
          "command": "claude-code-proxy.newConfig",
          "when": "view == claude-code-proxy.configs && viewItem == group-global",
          "group": "inline@1"
        },
        {
          "command": "claude-code-proxy.newLocalConfig",
          "when": "view == claude-code-proxy.configs && viewItem == group-local && workspaceFolderCount > 0",
          "group": "inline@1"
        },
        {
          "command": "claude-code-proxy.importGlobalToLocal",
          "when": "view == claude-code-proxy.configs && viewItem == group-local && workspaceFolderCount > 0",
          "group": "inline@2"
        },
        {
          "command": "claude-code-proxy.switchLocalConfig",
          "when": "view == claude-code-proxy.configs && viewItem == local-config",
          "group": "inline@1"
        },
        {
          "command": "claude-code-proxy.editLocalConfig",
          "when": "view == claude-code-proxy.configs && viewItem == local-config",
          "group": "inline@2"
        },
        {
          "command": "claude-code-proxy.deleteLocalConfig",
          "when": "view == claude-code-proxy.configs && viewItem == local-config",
          "group": "inline@3"
        }
      ]
    },
    "commands": [
      {
        "command": "claude-code-proxy.newConfig",
        "title": "New LLM Config",
        "icon": "$(add)"
      },
      {
        "command": "claude-code-proxy.editConfig",
        "title": "Edit",
        "icon": "$(edit)"
      },
      {
        "command": "claude-code-proxy.switchConfig",
        "title": "Switch to This Config",
        "icon": "$(arrow-swap)"
      },
      {
        "command": "claude-code-proxy.deleteConfig",
        "title": "Delete",
        "icon": "$(trash)"
      },
      {
        "command": "claude-code-proxy.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "claude-code-proxy.openView",
        "title": "Open Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.openProxyUI",
        "title": "LLM 代理: 打开控制台（重试参数 + Trace）",
        "category": "Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.killProxy",
        "title": "LLM 代理: 重启代理（关闭监听，宿主心跳自动重起）",
        "category": "Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.toggleProxyBackup",
        "title": "LLM 代理: 切换本窗口 backup proxy 开关",
        "category": "Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.toggleProxyBackupOn",
        "title": "LLM 代理: 开启本窗口 backup proxy",
        "icon": "$(debug-start)",
        "enablement": "!claude-code-proxy.proxyToggleEnabled"
      },
      {
        "command": "claude-code-proxy.toggleProxyBackupOff",
        "title": "LLM 代理: 关闭本窗口 backup proxy",
        "icon": "$(debug-stop)",
        "enablement": "claude-code-proxy.proxyToggleEnabled"
      },
      {
        "command": "claude-code-proxy.diagProxyHttp",
        "title": "LLM 代理: 诊断 proxy-agent 劫持（http.get 探针）",
        "category": "Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.exportConfigs",
        "title": "Export Configs",
        "icon": "$(export)"
      },
      {
        "command": "claude-code-proxy.importConfigs",
        "title": "Import Configs",
        "icon": "$(cloud-download)"
      },
      {
        "command": "claude-code-proxy.launchWorkspaceClaude",
        "title": "Launch Workspace-Isolated Claude",
        "icon": "$(terminal)",
        "category": "Claude Code Proxy"
      },
      {
        "command": "claude-code-proxy.newLocalConfig",
        "title": "New Workspace-Local Config",
        "icon": "$(add)"
      },
      {
        "command": "claude-code-proxy.editLocalConfig",
        "title": "Edit (Local)",
        "icon": "$(edit)"
      },
      {
        "command": "claude-code-proxy.switchLocalConfig",
        "title": "Switch to This Local Config",
        "icon": "$(arrow-swap)"
      },
      {
        "command": "claude-code-proxy.deleteLocalConfig",
        "title": "Delete (Local)",
        "icon": "$(trash)"
      },
      {
        "command": "claude-code-proxy.importGlobalToLocal",
        "title": "Import All Global Configs to Workspace Local",
        "icon": "$(arrow-down)"
      }
    ],
    "configuration": {
      "title": "Claude Code Proxy",
      "properties": {
        "claude-code-proxy.configFilePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Override the Claude Code settings.json path. Leave empty to auto-detect (`~/.claude/settings.json` on all platforms, including WSL)."
        },
        "claude-code-proxy.claudeBinaryPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Override the Claude Code CLI binary path. Leave empty to auto-detect from the installed `anthropic.claude-code` extension (`resources/native-binary/claude.exe` on Windows, `resources/native-binary/claude` on Linux/macOS)."
        }
      }
    },
    "keybindings": [
      {
        "command": "claude-code-proxy.launchWorkspaceClaude",
        "key": "ctrl+shift+alt+c",
        "mac": "cmd+shift+alt+c",
        "when": "workspaceFolderCount > 0"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "prepublishOnly": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test:mock-cli": "node --test test/mock-cli/test/",
    "test:e2e": "playwright test --config=playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.80.0",
    "chokidar": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "ws": "^8.21.1"
  }
}
